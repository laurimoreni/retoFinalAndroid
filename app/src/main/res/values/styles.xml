<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:actionMenuTextColor">@color/colorWhite</item>
        <item name="android:actionBarStyle">@style/AppTheme.ActionBarStyle</item>
        <item name="android:actionOverflowButtonStyle">@style/AppTheme.MyActionButtonOverflow</item>
        <item name="android:textColor">#000000</item>
    </style>

    <style name="AppTheme.ActionBarStyle" parent="Theme.AppCompat.Light">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:titleTextStyle">@style/AppTheme.ActionBar.TitleTextStyle</item>
    </style>

    <style name="AppTheme.ActionBar.TitleTextStyle" parent="Theme.AppCompat.Light">
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="AppTheme.MyActionButtonOverflow" parent="Theme.AppCompat.Light">
        <item name="android:src">@mipmap/baseline_more_vert_white_18dp</item>
    </style>

    <style name="TextColor">
        <item name="android:textColor">#00FF00</item>
    </style>

</resources>
